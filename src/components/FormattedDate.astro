---
interface Props {
  date: Date;
  locale?: string;
}

const { date, locale = 'es-ES' } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString(locale, {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
